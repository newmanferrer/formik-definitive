//* ///////////////////////////////////////////////////////////////////////////
//* FormInputRadChe
//* ///////////////////////////////////////////////////////////////////////////
//* ===========================================================================
//* 1.- Imports
//* ===========================================================================
import styled from 'styled-components';
import { COLORS } from '../../colors';
import { Field } from 'formik';
//* ===========================================================================

//* ===========================================================================
//* 2.- Reusable Components
//* ===========================================================================
//* 2.1.-
//* ---------------------------------------------------------------------------
//* ---------------------------------------------------------------------------
//* ===========================================================================

//* ===========================================================================
//* 3.- Used components
//* ===========================================================================
//* ---------------------------------------------------------------------------
//* 3.1.- Formik Field Styled (Formik Field component styled)
//* ---------------------------------------------------------------------------
const FormikFieldStyled = styled(Field)`
 appearance: none;
 visibility: hidden;
 position: absolute;
 left: 0;
`;
//* ---------------------------------------------------------------------------

//* ---------------------------------------------------------------------------
//* 3.2.- Circle Image
//* ---------------------------------------------------------------------------
const Figure = styled.i`
 height: 16px;
 width: 16px;

 position: absolute;
 top: 4px;
 right: 0;

 transition: all 0.3s ease-in-out;

 border-radius: ${({ figuretype }) => (figuretype === 'radio' ? '50%' : '10%')};
 border: 2px solid ${COLORS.borderRadChe};
 background: ${COLORS.white};

 &::before {
  height: 10px;
  width: 10px;

  content: '';
  display: block;

  position: absolute;
  top: 1px;
  left: 1px;

  transition: all 0.3s ease-in-out;
  transform: scale(0);
  opacity: 0;

  background: ${COLORS.hover};
  border-radius: ${({ figuretype }) => (figuretype === 'radio' ? '50%' : '10%')};
 }
`;
//* ---------------------------------------------------------------------------

//* ---------------------------------------------------------------------------
//* 3.1.- Label (works as a container for input radio or checkbox)
//* ---------------------------------------------------------------------------
const Label = styled.label`
 padding-right: 25px;
 position: relative;
 font-family: serif;
 font-size: 1.2rem;
 color: ${COLORS.white};

 &:hover {
  cursor: pointer;
 }

 ${FormikFieldStyled}:checked + ${Figure}::before {
  transform: scale(1);
  opacity: 1;
 }

 &:hover ${FormikFieldStyled}:not(:checked) + ${Figure} {
  background-color: ${COLORS.backgroundRadChe};
 }
`;
//* ---------------------------------------------------------------------------
//* ===========================================================================

//* ===========================================================================
//* 4.- Exported components
//* ===========================================================================
export { Label, FormikFieldStyled, Figure };
//* ===========================================================================
//* ///////////////////////////////////////////////////////////////////////////
