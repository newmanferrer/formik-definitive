import { MainContainer } from './StyledComponents';
import { FormLogin } from '../../components';

export const LoginPage = () => {
 return (
  <MainContainer>
   <FormLogin />
  </MainContainer>
 );
};
